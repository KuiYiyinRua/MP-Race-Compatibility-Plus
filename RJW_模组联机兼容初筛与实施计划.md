# RJW 模组联机兼容初筛与实施计划

> 范围：`H:\下载\元之雨RJW全种族拓展\rim\rim\rimworld\Mods` 下**第一层目录名包含 `RJW`** 的 112 个目录。
>
> 日期：2026-08-04；性质：静态初筛与实施排期，尚未运行游戏或对任一 DLL 做反编译。结论应作为排优先级依据，不应视作“已通过联机测试”。

## 结论摘要

RimWorld Multiplayer（下称 MP）复制的是命令并在各端确定性重放，而非持续同步所有状态。因此，新增 Def、贴图、XML Patch 本身通常不需要写专项 MP 补丁；真正的风险来自**本地 UI 回调、随机事件/生产、世界或地图 Tick、任务/交易、关系/生殖数据、静态缓存和未排序集合**。

本批目录应以“核心框架与主动玩法”优先，而不是逐个给所有 RJW 附属包写补丁：

| 分层 | 含义 | 处理决定 |
| --- | --- | --- |
| P0 | 16 个核心或高扇出玩法 | 逐个做源码/反编译审计、专项补丁和主机/客户端验证。 |
| P1 | 其余带 DLL 或会主动改写游戏状态的内容包 | 先在 P0 稳定后做定向审计；只有发现未覆盖的本地变更才写补丁。 |
| P2 | Def、种族、理念、服装或兼容 XML 包 | 不预设写专项代码；纳入一致性加载、生成/使用和 10k tick 冒烟。 |
| P3 | 纯纹理、动画数据或静态展示包 | 不需要专项同步；只保证双方版本、文件和加载顺序完全一致。 |

“不需要专项兼容”不等于能任意混装：所有玩家仍必须使用相同包 ID、版本、DLL、Defs 与加载顺序；若 P2/P3 包的前置模组本身有未解决的同步问题，它们不能消除该问题。

## 初筛依据与局限

已对每个目录读取 `About.xml`（若存在），并盘点 `Assemblies/*.dll`、`Defs` 和 `Patches`。本次发现 112 个匹配目录，其中不少没有 DLL，或仅包含资源/XML。已检索本地 `Multiplayer-Compatibility`，未发现以 `rim.job.world`、`rjw.menstruation`、`rjw.sexperience`、`c0ffee.rimworld.animations`、`sizedapparel` 为键的现成兼容实现；下一阶段仍须检查 MP 主仓库自身是否已覆盖具体命令路径。

静态信号只能判断风险，不能证明同步边界。特别是“有 DLL”不必然不兼容，“无 DLL”也可能通过 XML 启用某个有风险的前置功能。最终取舍以实际变更路径和双端运行证据为准。

## P0：必须优先做专项兼容

| 模组（目录） | 风险原因 | 预计深度 |
| --- | --- | --- |
| `RJW_核心_本体`（RimJobWorld） | 30 个 DLL；核心工作、Hediff、关系和性交互状态 | 完整动作清单、核心同步、长时 soak |
| `RJW_功能_性经验核心`（RJW Sexperience） | 经验/技能数据、奖励和 UI 操作 | 完整审计与专项同步 |
| `RJW_动画_2.0核心框架`（Rimworld Animations 2.0） | 动画驱动的实际动作链和回调 | 确认动作边界；避免只同步窗口 |
| `RJW-玩法-月经周期（RJW Menstruation Cycle）` | 13 DLL、周期 Tick、怀孕/产物/状态 | 完整 Tick/随机/生殖状态审计 |
| `RJW-Sar0-本体核心`（Sized Apparel for RJW） | 4 DLL，服装尺寸/身体状态和可能的缓存 | 装备/生成/重算路径审计 |
| `RJW_Onahole色情束缚家具` | 26 DLL、家具 gizmo、使用者/物品状态 | 所有 gizmo/目标选择/重复操作审计 |
| `RJW_功能_重金属冶炼核心`（RJW PE） | 10 DLL、关系/年龄限制相关游戏机制 | 完整动作与状态审计 |
| `RJW-SexSlaveCraft-星怒工艺` | 工作、奴隶/殖民者状态和物品生产 | 工作下达与产物确定性审计 |
| `RJW-功能-色情事件`（RJW-Events） | 6 DLL、事件随机性、信件/生成物 | 事件随机、信件和延迟动作审计 |
| `RJW-功能-色情商人`（RJW Ero Traders） | 交易 UI、库存、银币和 Pawn 生成 | 交易确认命令与生成顺序审计 |
| `RJW_文化玩法_妓院殖民地-1.6-test` | 经营/任务、工作分配、建筑和关系状态 | 端到端业务流和多次确认测试 |
| `RJW-体液-精液汇编（Cumpilation）` | 2 DLL、14 Def/Patch，体液与状态/产物链 | 产物、Hediff、随机和清理路径审计 |
| `RJW_基因_核心`（RJW Genes） | 基因赋予/遗传、Pawn 生成与 UI | 基因编辑、出生/生成、序列化审计 |
| `RJW-基因-奇幻种族基因（Fantasy Races）` | 5 DLL、种族/基因生成和多前置交互 | 与 Genes 一起覆盖生成与编辑 |
| `RJW_功能_浪漫调整更多选项` | 8 DLL，本地菜单/关系修改的典型风险 | 每个菜单和确认回调逐一盘点 |
| `RJW_补丁_边缘爱情浪漫PE版` | 37 DLL，且会改写关系/恋爱流程 | 独立高风险审计；先验证其 DLL 来源与实际加载 |

P0 的共同验收深度：每个会改变模拟状态的入口都要找到稳定执行点；检查 MP 是否已捕获；必要时只同步该动作/结果并携带 Pawn、Map、Faction、目标、数量等耐久标识。不得同步窗口打开、选中状态、动画播放或音效。所有随机必须在同步命令中执行，并对集合排序。

## P1：需要定向审计，可能需要专项补丁

以下包含程序集或明显主动玩法。优先按“实际在当前联机整合包启用、玩家可手动触发、会生成 Pawn/物品/事件”的顺序处理。

| 类别 | 目录 |
| --- | --- |
| 关系、性交互、Hediff、性格 | `rjw-做爱感想-紧致度`、`rjw-做爱感想-贤者时间`、`rjw-做爱感想-颜值`、`RJW-功能-ElToro的拉伸机制`、`RJW-功能-睾丸和卵巢`、`RJW-功能-裸体评价`、`RJW-功能-金属怪形性传播`、`RJW-机制-边缘巴士`、`RJW多重高潮`、`RJW_功能_我想要乱伦`、`RJW_扩展_家庭关系扩展`、`RJW_机制_小妾项圈` |
| 基因、种族、遗传 | `RJW-基因-RaddusX的魔鬼`、`RJW-基因-性技拓展`、`RJW-恋尸癖与螈尸的补丁`、`RJW_基因_子扩展_兽交动物遗传`、`RJW_扩展_苗床巨蝇`、`RJW_种族派系_奶牛人`、`RJW-种族-畸瘤体`、`rjw-elt-hdryad-Modded` |
| 理念、任务、事件、社交 | `RJW-文化-性别主义`、`RJW-文化-殖民地的妓院任务补丁`、`RJW-文化-淫秽文化`、`RJW_文化_咖啡的人奶文化`、`RJW_文化玩法_隐私意识`、`RJW-玩法-肮脏的谈话` |
| 性解放/性恶魔及玩法扩展 | `RJW-玩法-性解放框架`、`RJW-玩法-性解放淫纹`、`RJW-玩法-性解放特质`、`RJW-玩法-性解放美食`、`RJW_玩法-性恶魔-核心`、`RJW_玩法-性恶魔-成瘾品`、`RJW_玩法-性恶魔-魅魔转化剂`、`RJW_玩法_ElToro的人兽杂交`、`RJW_玩法_ElToro的机性恋`、`RJW_玩法_SM道具+炮机`、`RJW-灵能-大量RJW扩展` |
| 月经/资源/物品/家具 | `RJW-玩法-月经周期-资源`、`RJW-玩法-月经周期-液体贴图`、`RJW-体液-妹汁`、`RJW-体液-奶基药物`、`RJW-装饰-s16的性玩具`、`RJW-装饰-妓女床`、`RJW_纹理_家具妓院招牌-1.0.7.0`、`RJW_纹理_边缘裸体_动物` |
| 动画运行时包 | `RJW_动画_UAP终极动画包`、`RJW_动画_犬科动物动画`、`RJW_动画_通用动物动画` |
| 异常项 | `RJW_补丁_受虐狂不减压制`（149 DLL，先确认其装配结构/是否有重复或旧版程序集） |

P1 的默认深度是“先证伪”：阅读源码或反编译，列出所有 `Command_Action`、`FloatMenuOption`、`Dialog/DiaOption`、`Widgets.Button*`、Gizmo、事件触发和 Tick；与 MP 源码对照后，仅为未被自动捕获且会改模拟状态的路径注册最窄同步。若所有状态变更均由已有同步工作/命令触发，则记录为“兼容，无新增代码”。

## P2：默认无需专项代码，需作为联机载荷测试

这些包以 XML Def、Patch、种族定义、理念、服装或静态兼容为主。它们可能改变内容，但没有足够的静态信号表明存在独立本地命令边界。应在其前置 P0/P1 通过后，随整合包测试生成、装备、交互和保存/重连。

`RJW-FA 支持`、`RJW-SA0-真空服SA0`、`RJW-SA0-莫诺灵sa`、`RJW-Sar0-aya人工种族SA`、`RJW-Sar0-Realistic身形`、`RJW-Sar0-YUKI魔物娘SA1.6`、`RJW-Sar0-噤声lamia girl sa`、`RJW-Sar0-悠兰月经生殖器兼容`、`RJW-Sar0-烟烬SA`、`RJW-Sar0-织Spider girl sa`、`RJW-功能-OTY的RJW互动符号`、`RJW-功能-Chill RJW`、`RJW-基因-性技扩展-SA0补丁`、`RJW-基因-更好的近亲繁殖基因`、`RJW-小功能-淫荡的交互文本`、`RJW-文化-扶她文化`、`RJW-服装-AKT_LewdClothes`、`RJW-服装-chaerens的服装`、`RJW-服装-SA奴隶服装`、`RJW-服装-胶衣`、`RJW-玩法-月经周期-外星人胎儿纹理`、`RJW-玩法-月经周期-贴图重置`、`RJW-玩法-爱神触手Amor-tentaculum-main`、`RJW-纹身-罗博士的羞辱包`、`RJW-装饰-Mustard的色情雕像`、`RJW-装饰-SCC 更多雕塑`、`RJW_动画_3p动画`、`RJW_动画_Hawk的动画`、`RJW_动画_泛用型动物动画插件-SeXtra2`、`RJW_动画_男性发起动物动画`、`RJW_动画_调整 HAR 播放偏移量`、`RJW_动画九莲支持`、`RJW_原版分离_昆虫器官和卵`、`RJW_外星人月经卫生巾支持补丁`、`RJW_文化_好色文化起源风格`、`RJW_文化风格_恋兽癖`、`RJW_玩法_ElToro的失能机关合成机兵UAP动画开腿支持+机性恋支持`、`RJW_种族派系_性解放_丑陋杂种`、`RJW_种族派系_性解放_魅魔部落`、`RJW_补丁_S16道具pe补丁`、`RJW_补丁_月经pe补丁儿童卫生用品`、`RJW_补丁_边缘爱情RJW适配补丁`。

## P3：不需要专项同步，仅保持文件一致

以下包表现为贴图/重贴图/静态资源，或仅为动画数据；不应给它们添加 `SyncMethod`、同步 UI 或对视觉代码打补丁：

`RJW_纹理s16拓展的玩具贴图拓展`、`RJW_纹理_Kilo的兽交UI替换`、`RJW_纹理_娘化动物纹理【无需RJW环境】`、`RJW_纹理_家具妓院招牌-1.0.7.0` 的纯资源部分、`RJW-玩法-月经周期-贴图重置`、`RJW-玩法-月经周期-外星人胎儿纹理`、`RJW_动画_3p动画`、`RJW_动画_Hawk的动画`、`RJW_动画_男性发起动物动画`、`RJW_动画九莲支持`、`异常娘化18+工坊混合版【无需RJW环境】`。其余“无 DLL 的动画/服装/补丁”亦按 P2 处理。

> 注：个别包（例如带 DLL 的招牌、动物图形包）同时含资源与运行时代码，分类以运行时代码为准，故仍保留在 P1；此处不把它们误判成纯贴图。

## 实施方案

### 阶段 0：锁定测试基线（0.5–1 天）

1. 从实际启用的 `ModsConfig.xml` 导出子集，而不是假定 112 个包全部同时启用。
2. 记录每个启用包的 packageId、版本、加载顺序、DLL SHA-256，以及 RimWorld/MP/Harmony 版本。
3. 建立隔离的 host/client `-savedatafolder`；两端只使用复制出的相同文件，不写入玩家现有存档或配置。
4. 用最小 P0 组合先完成一次“可进服、两人 `ClientPlaying`、无 Def mismatch”的基准存档。

### 阶段 1：核心同步边界（优先顺序：P0，约 4–8 天）

按以下簇逐一审计并测试，前一簇无确定性问题后再加入下一簇：

1. RJW 核心 + Sexperience + Animations 2.0；
2. Menstruation Cycle + Resources/Fluids + Genes；
3. Sized Apparel + 服装/种族支持；
4. Onahole + S16 + SM/家具；
5. PE + Romance Tweaks + 关系扩展；
6. 事件、商人、妓院殖民地、任务及 Cumpilation。

每一簇的工作产物：源码权威/程序集哈希记录、动作清单、MP 已有覆盖结论、所需补丁、补丁目标解析日志、测试记录。若没有源码，先比对已安装 DLL 的元数据和公开源码；仍无匹配才在 `BuildValidation/Decompiled` 反编译，绝不直接修改目标 DLL。

### 阶段 2：P1 按需补齐（约 3–6 天）

对每个已启用 P1 包，优先审计非主机玩家发起的 UI 与事件路径。典型同步边界是“确认交易”“应用关系/特质”“开始工作/互动”“生成确定的事件结果”，而不是菜单、窗口、动画、提示音。参数不能直接序列化时，用 Thing/Pawn ID、Map、Faction 等稳定标识写 `SyncWorker` 重建上下文。

### 阶段 3：内容回归与缩减（约 1–3 天）

逐批加入 P2/P3（先同一前置的包，再加动画与重贴图），每批完成：生成新 Pawn/种族、装备相关物品、触发可用设施或 Def、存档/重载、断线重连。若一批出错，二分拆分到具体包，再提升至 P1 审计；不要先给整个类别添加宽泛 Harmony patch。

## 每个补丁的验收门槛

1. 编译目标解析在两端全部成功，且没有重复同步或反射命中错误重载。
2. 两端使用相同候选 DLL 哈希，主机出现 `Server started.`，非主机达到 `ClientPlaying` 且玩家数为 2。
3. 由非主机走真实 UI 链执行目标动作至少 3 次；包含首次/关闭重开/取消后重开；涉及地图或世界上下文时覆盖两者。
4. 两端断言相关 Pawn/Thing ID、Hediff/关系/库存/银币/物品生成/信件/任务状态一致。
5. 目标动作后运行约 10,000 个共享 tick 冒烟；P0 核心簇再运行至少 120,000 个共享 tick soak。任何 `Sync Error`、包错误、断线、状态断言失败或 desync 都是失败，不以“没有弹窗”代替验证。
6. 将最终测试过的 DLL 归档并复核 SHA-256；代码或 DLL 改动后重新测试。

## 交付与风险控制

建议把兼容代码集中在当前 `MP-meow-online-shop`，按目标 mod 分文件/注册类，避免修改第三方 RJW DLL。每个兼容项保留：目标包 ID/版本/哈希、动作入口、同步边界、传输上下文、所有副作用、三次操作证据、短测/soak 结果和回退方式。

最高风险不是视觉包，而是一次性将全套 RJW 子模组加入联机：核心状态机、周期 Tick、任务/事件、交易和关系修改叠加后很难定位。应坚持“最小可联机基线 → 单簇 → 单包”的递增顺序；未通过 P0 的簇在正式联机包中保持禁用。
